# Дорожная карта проекта "Тренажер дыхания"

## Текущие возможности (Реализовано)

### 1. Режимы тренировок
*   **Свободный режим**: Настройка времени вдоха, выдоха и задержек (0-18 сек).
*   **Метод Вима Хофа**:
    *   Специальная логика фаз: Вдохи/Выдохи -> Задержка (Retention) -> Восстановление (Recovery).
    *   **Умная шкала задержки**: 
        *   От 30 до 300 секунд (5 мин) — шаг **10 секунд**.
        *   От 5 до 20 минут (1200 сек) — шаг **1 минута**.
    *   Настройка количества циклов (от 1 до 10).
*   **Готовые пресеты (21 техника)**: Отображение настроек (например, "4 4 4 4") рядом с названием в списке.

### 2. Звук и Музыка
*   **Интеллектуальный звук дыхания**: Автоматический подбор аудиофайла в зависимости от длительности фазы.
*   **Фоновая музыка**: 6 встроенных медитативных треков + возможность добавления своей музыки.

### 3. Интерфейс и Анимация
*   **Экран дыхания**: 
    *   Отображение названия техники и параметров (4-4-4-4) в заголовке.
    *   Счетчик текущего круга для всех режимов.
    *   Управление (Play/Pause) перенесено вниз, убрана лишняя графика.
*   **Нижняя панель**: Закрепленные кнопки Info и Назад, не перекрываемые рекламой или системными кнопками.
*   **Wake Lock**: Экран не гаснет во время работы приложения.

### 4. Облачные функции и Интеграция
*   **Авторизация**: Google, Яндекс и VK ID через Firebase Authentication; токены Яндекс/VK проверяются доверенной Cloud Function.
*   **Синхронизация**: Настройки, встроенная музыка, пресеты, история и расписание напоминаний синхронизируются через **Firebase Firestore**.
*   **Yandex Ads**: Баннеры и межстраничная реклама (исправлен вызов перед стартом).
*   **RuStore Pay**: Поддержка покупок и подписок через RuStore SDK v10.2.0.

---

## Актуальный план доработок и исправлений

1.  [x] **Повышение версии приложения**: Увеличение `versionCode` до `2` и `versionName` до `1.1` для беспроблемной загрузки новых сборок в сторы.
2.  [x] **Диагностика и исправление платежей RuStore**:
    *   Исправлена доставка Deeplink от банка при возврате с оплаты (`MainActivity.kt` переведен на `by viewModels()`).
    *   Подготовлен чек-лист настройки консоли RuStore (Console App ID, SHA-256 отпечаток, создание продуктов `sub_monthly` и `premium_lifetime`).
3.  [x] **Интеграция авторизации через Яндекс и VK ID**:
    *   Добавлены интерфейсные кнопки входа через **Яндекс** и **VK ID** в окне настроек/синхронизации (в соответствии с требованиями РФ).
    *   Реализована обработка входа и синхронизация аккаунта в `BreathingViewModel`.
4.  [x] **Виджет для рабочего стола Android**.

## Аудит стабильности и безопасности — план исправлений

Пункты выполняются последовательно. Каждый пункт считается готовым только после сборки и релевантных автоматических тестов.

1.  [x] **Жизненный цикл тренировочной сессии**:
    *   Сделать завершение сессии идемпотентным и исключить дубли истории.
    *   Исключить ложную многолетнюю тренировку при холодном запуске из виджета.
    *   Реализовать настоящую паузу/продолжение без повторной рекламы и сброса прогресса.
    *   Не учитывать время паузы в длительности тренировки.
2.  [x] **Напоминания и разрешения уведомлений**:
    *   Использовать явный `BroadcastReceiver`, поддержать ежедневные и календарные напоминания.
    *   Запрашивать `POST_NOTIFICATIONS`, восстанавливать будильники после перезагрузки и использовать уникальные `PendingIntent`.
3.  [x] **Доверенная проверка Premium**:
    *   Убрать возможность записывать entitlement с клиента.
    *   Сделать RuStore/сервер источником истины и корректно обрабатывать отзыв покупки и ошибки проверки.
4.  [x] **Авторизация Яндекс и VK ID**:
    *   [x] Клиент отправляет внешний токен только на HTTPS backend и входит в Firebase через custom token.
    *   [x] Не показывать успешный вход при ошибке и не выводить части токенов/идентификаторов в UI или логи.
    *   [x] Развернуть backend-проверку токенов Яндекс/VK в `europe-west1` и задать `AUTH_EXCHANGE_URL` в конфигурации сборки.
    *   [x] Выдать runtime-аккаунту право подписи Firebase custom token и проверить реальные входы через Яндекс и VK ID на устройстве.
5.  [x] **Безопасная облачная синхронизация**:
    *   [x] Синхронизировать настройки, встроенную музыку, пресеты, историю и расписание напоминаний; локальные пользовательские аудиофайлы намеренно не отправлять в облако.
    *   [x] Устранить write-back race, объединять поля документа, добавить версию схемы, ограничение размеров и отбрасывание устаревших callback.
    *   [x] Разделить локальные профили Firebase-пользователей и гостя, безопасно мигрировать данные старой версии только в первый аккаунт.
    *   [x] Опубликовать серверную валидацию допустимых полей, типов и размеров в Firestore Rules.
    *   [x] Проверить реальный Google Sign-In на устройстве и показывать источник входа: Google, Яндекс или VK.
    *   [x] Проверить read-only instrumentation-тестом, что Google-профиль в Firestore содержит полную версионированную схему.
    *   [x] Проверить на устройстве смену Google → VK → Google и восстановление раздельных профилей.
6.  [x] **ANR и размер приложения**:
    *   [x] Удалить неиспользуемую Media3 UI-зависимость и избыточные R8 keep/dontwarn-правила; release-сборка проходит с полноценным shrink.
    *   [x] Включить удаление ресурсов, удалить пять неиспользуемых начертаний Inter и уменьшить release APK на 4,01 МБ (18,1%).
    *   [x] Отложить создание трёх ExoPlayer до первого воспроизведения и убрать повторную инициализацию Mobile Ads из Activity.
    *   [x] Изолировать виджет от UI SDK в лёгком процессе и проверить холодное обновление release-сборки на устройстве: 0,8–1,5 с, без ANR и крашей.
    *   [x] Проверить холодный запуск release Activity на устройстве: 740–809 мс, без ANR и крашей; Baseline Profile при таком результате не требуется.
7.  [x] **Надёжность таймеров и режимов**:
    *   [x] Использовать монотонное время, фиксировать параметры на старте, ограничить повторения/циклы и полностью сбрасывать состояние при выходе из режима Вима Хофа.
    *   [x] Отбрасывать пустые/повреждённые фазы; после дыхательного цикла Вима Хофа запускать неограниченный монотонный секундомер с нуля вверх.
    *   [x] Завершать задержку только по действию пользователя, атомарно сохранять фактическое время каждого круга, показывать личный рекорд в истории и исключить двойное добавление результата при повторном нажатии.
8.  [x] **Lifecycle аудио и рекламы**:
    *   [x] Добавить единый audio focus для всей тренировки, безопасную паузу при звонке/другом аудио и при уходе приложения в фон; временное системное прерывание возобновляет тренировку один раз, а ручная пауза и постоянная потеря фокуса — нет.
    *   [x] Обрабатывать синхронные и асинхронные ошибки всех трёх плееров без краша, не создавать `lazy`-плееры при паузе/остановке, освобождать таймеры, focus и плееры при уничтожении ViewModel. После уничтожения процесса незавершённая сессия безопасно завершается и не записывается как завершённая.
    *   [x] Убрать двойной вызов рекламы при старте и сделать показ single-flight с 5-секундным тайм-аутом, lifecycle-проверками и однократным callback.
9.  [x] **Privacy и защита локальных данных**:
    *   [x] До создания экранов с рекламой показывать недизаблимый выбор privacy с выключенными по умолчанию аналитикой и персонализацией; запускать Yandex Ads/AppMetrica только после сохранения выбора, отключить автоинициализирующие `ContentProvider` и разрешить менять выбор в настройках.
    *   [x] Удалять вызовы `android.util.Log` из release через R8 и запретить Auto Backup/Data Transfer для локальных preferences, баз, файлов, токенов и истории; восстановление поддерживаемых данных остаётся через раздельный Firestore-профиль после входа.
10. [x] **Сборка, секреты и deep links**:
    *   [x] Убрать VK client secret из APK: новый клиент использует OAuth 2.1 Authorization Code + PKCE, а обмен кода выполняет доверенная Cloud Function; старый сценарий временно сохранён на backend для уже установленных версий.
    *   [x] Валидировать обязательную production-конфигурацию перед release-сборкой и поддержать одинаковое чтение значений из Gradle properties, переменных окружения и `local.properties` для последующего CI.
    *   [x] Обрабатывать только `ACTION_VIEW` с допустимой схемой и безопасным размером/форматом RuStore deep link; двойная инициализация Mobile Ads устранена.
    *   Операционный шаг после обновления основной доли пользователей: перевыпустить старый VK protected key. До rollout этого делать нельзя, иначе VK-вход сломается в предыдущих версиях приложения.
11. [x] **Manifest, разрешения и совместимость устройств**:
    *   [x] Удалить необязательные Bluetooth-разрешения, явно пометить сенсорный экран optional, запретить backup также флагом приложения и убрать лишние Manifest-атрибуты.
    *   [x] Перейти на `compileSdk/targetSdk 36` и совместимый проверяемый toolchain Gradle 8.11.1 / AGP 8.10.1.
    *   [x] Снять портретную блокировку, адаптировать центральную тренировочную область к малой высоте и использовать современный edge-to-edge API системных панелей.
    *   [x] Разделить метаданные виджета по API, убрать nested weights и hardcoded-строки, исправить launcher/density-ресурсы и удалить неиспользуемые изображения.
    *   [x] Устранить все локальные предупреждения Lint; оставшиеся уведомления относятся только к доступности новых версий сторонних зависимостей и требуют отдельных регрессионных миграций.
12. [x] **Целостность данных и UX**:
    *   [x] Вычислять streak по локальным календарным датам из объединённой истории, без вычитания фиксированных 24 часов и без восстановления устаревшего облачного счётчика; покрыть DST, повторные тренировки за день и пропущенный день unit-тестами.
    *   [x] Ограничивать историю последними 100 записями не только в preferences/Firestore, но и при добавлении и загрузке в память; ограничивать и валидировать вложенные результаты задержек.
    *   [x] После загрузки локальных пользовательских треков повторно разрешать сохранённый `selected_music_id`, а удалённый/недоступный идентификатор безопасно заменять стандартным треком.
    *   [x] Показывать реальный `BuildConfig.VERSION_NAME` и формировать описания встроенных техник из тех же параметров, которые создают их фазы.
13. [x] **Автоматические тесты и release-gate**:
    *   [x] Покрыть таймер, историю, напоминания, авторизацию, синхронизацию и Premium unit/instrumentation-тестами.
    *   [x] Заменить устаревший Google Sign-In на Credential Manager, сохранив прежние Firebase-проект, Web Client ID и обмен ID token через Firebase Auth.
    *   [x] На устройстве повторно войти тем же Google-аккаунтом и подтвердить прежний профиль/историю; автоматическая проверка не должна выходить из реального аккаунта пользователя.
    *   [x] Включить обязательные тесты Cloud Functions/Android, `lint` и сборку release-варианта в GitHub Actions; CI использует отдельную непроизводственную Firebase-конфигурацию только для компиляции.
14. [x] **Долговечность пользовательских аудиофайлов**:
    *   [x] Освобождать постоянное Android-разрешение на удалённый трек или заменённый пользовательский финальный звук, чтобы не исчерпать системный лимит URI grants.
    *   [x] Перед отзывом проверять активный и все сохранённые локальные профили; при обновлении безопасно удалить уже накопившиеся неиспользуемые grants.
    *   [x] Покрыть политику очистки unit-тестами и повторно пройти `test`, `lint`, debug/release-сборку и запуск на устройстве.
15. [x] **Политика конфиденциальности и публичные ссылки**:
    *   [x] Подготовить политику по фактической обработке локальных и облачных данных, авторизации, покупок, рекламы и аналитики.
    *   [x] Опубликовать политику на отдельном Firebase Hosting-сайте и проверить публичный HTTPS-адрес.
    *   [x] Добавить ссылку на политику в первоначальный privacy-диалог, настройки и окно информации.
    *   [x] Сделать «Лавку приложений» кликабельной и связать со страницей разработчика в RuStore; внешние ссылки открывать без краша при отсутствии обработчика.
